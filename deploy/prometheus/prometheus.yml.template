# Placeholder replaced at container start so self-scrape matches Render's $PORT.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["__SCRAPE_TARGET__"]
